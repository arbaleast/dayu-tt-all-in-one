# 大鱼 TT 项目安全审计报告

> 审计时间：2026-03-29
> 审计范围：/vol1/1000/projects/3d-printing/dayu-tt-all-in-one/

---

## 一、敏感信息检查

### 1.1 硬编码凭证扫描

| 检查项 | 结果 | 详情 |
|--------|------|------|
| GitHub Token (`ghp_`) | ✅ 未发现 | |
| OpenAI API Key (`sk-`) | ✅ 未发现 | |
| Google API Key (`AIza`) | ✅ 未发现 | |
| 硬编码密码 | ✅ 未发现 | 仅 `your_password` 占位符 |
| Bot Token (`7704135045:AAHdYe`) | ✅ 未发现 | 已从历史中清除 |
| Gateway Token (`c7f8727b71f...`) | ✅ 未发现 | 已从历史中清除 |

**扫描方法：**
```bash
grep -rsl "ghp_\|sk-\|AIza\|password\s*[:=]\|secret\s*[:=]" \
  --include="*.md" --include="*.yaml" --include="*.yml" \
  --include="*.json" --include="*.sh" .
```

**发现问题文件：**
- `docs/00-入门指南/2-软件固件配置.md:189` — 包含 `sudo_password: your_password`（占位符，无风险）

---

### 1.2 Git 历史清理确认

| 检查项 | 结果 |
|--------|------|
| 旧仓库名 (`dayu-tt-enclosure`) | ✅ 已全部替换为 `dayu-tt-all-in-one` |
| 敏感文件历史 | ✅ 已通过 `git filter-repo` 清除 |
| GitHub Token 历史 | ✅ 已清除 |
| 运营脚本历史 | ✅ 已删除并从历史移除 |

**当前 git log（最近 5 条）：**
```
c9262c6 docs: 移除 scripts 目录
31c0e76 docs: 添加系统一键恢复脚本
25323c4 feat: AI 深度调研报告
e33531f fix: 修复 README 引用错误
53a253e feat: Phase3 增加振动传感器方案
```

---

### 1.3 .git 目录检查

| 检查项 | 结果 |
|--------|------|
| `.orig` / `.bak` / `.swp` 文件 | ✅ 未发现 |
| 敏感文件残留 | ✅ 未发现 |

---

## 二、文件权限检查

### 2.1 项目目录权限

```bash
ls -la /vol1/1000/projects/3d-printing/dayu-tt-all-in-one/
```

| 目录/文件 | 权限 | 风险 |
|-----------|------|------|
| `.git/` | drwxrwx-- | ⚠️ 组可写，应限制为 700 |
| `docs/` | drwxrwxr-x | ✅ 正常 |
| `bom/` | drwxrwxr-x | ✅ 正常 |
| `source/` | drwxrwxr-x | ✅ 正常 |

---

## 三、配置文件检查

### 3.1 docker-compose 敏感信息

| 文件 | 敏感信息 | 风险 |
|------|---------|------|
| `config/mihomo/config.yaml` | ⚠️ 需检查 | 代理配置可能有 API 地址 |
| `config/firefly-iii/.env` | 如存在需检查 | 数据库密码 |
| `config/jellyfin/` | ✅ 无敏感信息 | |

---

## 四、运营文件清理确认

| 文件 | 原状态 | 现状态 |
|------|--------|--------|
| `GITHUB_SYNC.md` | 存在，含运营脚本 | ✅ 已删除 |
| `GITHUB_COMPLETE.md` | 存在 | ✅ 已删除 |
| `SSH_SETUP.md` | 存在，含 SSH 公钥 | ✅ 已删除 |
| `SSH_QUICK_START.md` | 存在 | ✅ 已删除 |
| `setup-ssh.sh` | 存在 | ✅ 已删除 |
| `sync.sh` | 存在 | ✅ 已删除 |
| `.github/` 目录 | 含 GitHub Actions 脚本 | ✅ 已删除 |
| `archive/` 目录 | 含历史结构文档 | ✅ 已删除 |

---

## 五、建议

### 5.1 项目目录权限

`.git/` 目录权限为 775（组可写），建议收紧：

```bash
chmod -R 700 /vol1/1000/projects/3d-printing/dayu-tt-all-in-one/.git
```

### 5.2 配置文件检查

如需更高安全等级，定期扫描：

```bash
# 扫描 docker-compose 中的密码
grep -r "password\|secret\|token" docker-compose*.yml config/**/*.yaml
```

---

## 六、结论

| 类别 | 状态 |
|------|------|
| 敏感信息泄露 | ✅ 无 |
| Git 历史泄露 | ✅ 已清理 |
| 运营脚本残留 | ✅ 已删除 |
| 目录权限 | ⚠️ .git 需收紧 |
| 文档引用正确性 | ✅ 已验证 |

**总体评估：项目安全状态良好，敏感信息已清理完毕。主要遗留问题是 `.git/` 目录权限过宽。**
